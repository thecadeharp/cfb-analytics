name: Settle Results - No CFBD

on:
  workflow_dispatch:

  # College football is played every day of the week at some point in the
  # season (midweek conference games, holiday games, bowls and CFP).
  # Run hourly, seven days a week. The results collector only stores finals,
  # and the settlement script only grades games that have prospective snapshots.
  schedule:
    - cron: "45 * * * * *"

permissions:
  contents: write

concurrency:
  group: settle-results-no-cfbd
  cancel-in-progress: false

jobs:
  settle:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Fetch all completed 2026 season results without CFBD
        env:
          CFB_SEASON: "2026"
        run: |
          python scripts/fetch_results_no_cfbd.py

      - name: Settle prospective projections
        run: |
          python scripts/settle_results.py

      - name: Commit settlement outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/results.json
          git add data/reports/settled_results.json
          git add data/reports/settled_snapshot_rows.csv

          if git diff --cached --quiet; then
            echo "No settlement changes to commit."
            exit 0
          fi

          git commit -m "Settle completed games and refresh model report"

          # Another analytics workflow may have committed while this job ran.
          # Rebase once before pushing so independent scheduled jobs do not
          # unnecessarily collide.
          git pull --rebase origin main
          git push origin main
