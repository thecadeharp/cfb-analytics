name: Score Engine V1.3 OOS

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy

      - name: Run V1.3 possession/scoring experiment
        run: |
          python scripts/score_engine_v13_oos.py

      - name: Commit V1.3 OOS report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/training/score_engine_v13_oos.json

          if git diff --cached --quiet; then
            echo "No V1.3 report changes to commit."
            exit 0
          fi

          git commit -m "Run Score Engine V1.3 OOS experiment"
          git push
